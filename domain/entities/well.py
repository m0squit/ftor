class Well(object):

    def __init__(self,
                 name,
                 radius,
                 type_completion,
                 type_boundaries):

        self.name = name
        self.radius = radius
        self.type_completion = type_completion
        self.type_boundaries = type_boundaries
